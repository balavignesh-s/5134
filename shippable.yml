language: node_js

node_js: 0.12
env:
  matrix:
    - one=1
    - two=2
    - three=3

build:
  ci:
    - echo "Hello, World!"
    - echo "This is master"
    - echo "This is ??????"
    - echo "Hello, World!"
