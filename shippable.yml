node_js:
  - '0.12'
env:
  global:
    - one=1
  matrix:
    - two=2
    - three=3
build:
  preci:
    - echo "is_fork $IS_FORK"
  ci:
    - echo "This is !"
integrations:
  hub:
    - integrationName: myDocker
      type: docker
