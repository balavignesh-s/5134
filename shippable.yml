language: node_js

node_js:
  - '0.12'
env:
  global:
    - one=1
    - two=2
  matrix:
    - a=1
build:
  preci: 
    - date -u
  ci:
    - echo "This is !"

integrations:
  notifications:
    - integrationName: "webhook"
      type: webhook
      on_start: always
      on_success: never
      payload:
        - one=$SHIPPABLE_ARTIFACTS_DIR
        - two=$BRANCH
        - three=three
      
    - integrationName: "webhook_1"
      type: webhook
      on_start: never
      on_success: always
      payload:
        - four=$SHIPPABLE_BUILD_DIR
        - five=$COMMIT
        - six=six
